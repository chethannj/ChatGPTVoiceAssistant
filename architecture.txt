Only Chatbot input of only text(Previous application)
User → Text-Streamlit → Python code → OpenAI GPT → Back to Streamlit → User

Chatbot enabled with Voice Assistant(Current)
User → Voice/Text → Streamlit → Python → OpenAI GPT → Reply → pyttsx3 → Spoken Output → User

┌────────────────────────┐
│       User / Viewer     │
│  🎤 Speaks  or  ⌨️ Types │
└───────────┬─────────────┘                            
            │
            ▼
┌──────────────────────────────────┐
│     Web Browser (Streamlit UI)   │
│  ──────────────────────────────  │
│  • Chat input box 💬             │
│  • Voice record 🎙️    │
│  • Display chat history 🧾        │
│     │
└───────────┬──────────────────────┘
            │  (Sends text / audio)
            ▼
┌──────────────────────────────────────────┐
│         Streamlit App (Python)           │
│  ──────────────────────────────────────  │
│  • If **voice input**:                   │
│     - Records mic audio (sounddevice)    │
│     - Saves `.wav` temporarily            │
│     - Transcribes 🎧 speech → text (Whisper) │
│                                            │
│  • If **text input**:                     │
│     - Directly uses user message          │
│                                            │
│  • Manages chat history (session_state)   │
│  • Sends message to OpenAI GPT model 🧠   │
└───────────┬───────────────────────────────┘
            │ (API call to GPT)
            ▼
┌──────────────────────────────────────────┐
│          OpenAI GPT Model (API)          │
│  ──────────────────────────────────────  │
│  • Processes system + user messages       │
│  • Understands context & tone             │
│  • Generates intelligent reply text 💬     │
└───────────┬───────────────────────────────┘
            │ (Response: text reply)
            ▼
┌──────────────────────────────────────────┐
│          Streamlit App (Python)          │
│  ──────────────────────────────────────  │
│  • Displays GPT text reply on screen 🧾   │
│  • Calls **pyttsx3** engine locally 🔊     │
│     → Converts text → spoken voice 🗣️     │
│  • Plays audio through system speakers    │
└───────────┬───────────────────────────────┘
            │ (Spoken output)
            ▼
┌────────────────────────┐
│       User / Viewer 🧏‍♂️ │
│  • Sees AI text reply 💬 │
│  • Hears AI speak reply 🔊│
└────────────────────────┘



🎤 Voice/Text Input                      
      ↓
🌐 Streamlit Frontend (Chat UI)
      ↓
🐍 Python Backend (App Logic)
      ↓
☁️ OpenAI GPT (Brain)
      ↓
💬 Text Reply
      ↓
🗣️ pyttsx3 (Text-to-Speech)
      ↓
🔊 Spoken Output to User


